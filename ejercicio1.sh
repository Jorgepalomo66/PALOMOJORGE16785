#!/bin/bash

archivo="Accidentes_ags_2021.csv"
word="ADOLFO"
# Verificar
 salida=$(awk '{print $0" "$18}') #falto agregar este comando al grep junto con el column
if (grep -i "$word" "$archivo") 

if(grep -i "$word" "$archivo")
then
echo "Adolfo no existe" $archivo

fi

